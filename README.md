Shiny-Sandbox
=============

#### shiny-handsontable

Implementation of handsontable as a shiny input.  
Very early stage.

To test it:

    setwd("./shiny-handsontable")
    library(shiny)
    runApp()
